apply plugin: 'groovy'

repositories {
    maven {
        url 'https://repo.gradle.org/gradle/libs-releases-local'
    }
    jcenter()
}

dependencies {
    compile project(':model')
    compile project(':export')
}
